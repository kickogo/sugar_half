有了终极开发指令，接下来就需要一份高效的**前端开发计划（Sprint Plan）**。

考虑到你使用的是 **Claude Code**，它的核心优势是**可以直接修改本地文件并自动执行一部分构建**。所以，我们的开发计划不能像传统开发那样拉长周期，而是要采用“小步快跑、逐个击破、即时预览”的敏捷模式。

以下是为你量身定制的 **3天像素级前端落地计划**，配合微信开发者工具的热重载，能让你用最快的速度看到完美复刻的精美 UI。

---

# 📅 "JENNY & FRIENDS" 小程序前端 3天落地计划

## 🏁 Day 1：基础设施与全局框架搭建（打好地基）

第一天的核心是让 Claude Code 帮我们建立起正确的骨架和全局样式，确保后续页面可以直接调用变量。

### 📌 阶段一：资源就位与项目清理（预计 1 小时）

1. **清理默认模板：** 让 Claude Code 删除初始云开发模板里自带的 `pages/index` 冗余代码，清空 `app.wxss`。
2. **素材导入：** 将你准备好的 TabBar 图标、Banner 图、蛋糕商品图放入指定的 `/image/` 各个子目录下。

### 📌 阶段二：全局样式注入与 TabBar 配置（预计 2 小时）

1. **配置全局变量：** 让 Claude 把视觉规范（主题粉、红棕色、硬投影样式）写入 `app.wxss`。
2. **打通底部导航：** 修改 `app.json`，配置好 5 个页面的路由，并关联好你放进去的 5 组 TabBar 图标。
3. **今日检查点：** 微信开发者工具里底部 5 个标签栏正常显示，点击能够正常切页（即使页面目前是空白）。

---

## 🎨 Day 2：攻克核心视觉页面（首页 + 全部商品）

第二天的任务最重，要拿下最考验 CSS 细节的两个流量入口页面。

### 📌 阶段三：首页（Index）像素级复刻（预计 3 小时）

1. **实现平铺背景与轮播：** 优先让 Claude 写出轮播组件，并完美平铺粉白棋盘格背景。
2. **编写灵魂组件（倾斜锯齿票据）：** 这是最难的一步。让 Claude Code 用伪元素写出撕边效果。
3. **底部气泡框：** 还原手绘粗线和块投影。

### 📌 阶段四：全部商品（Goods）网格流（预计 3 小时）

1. **顶部活动海报：** 引入荔枝季 Banner，调好 `margin` 和圆角。
2. **2列 Grid 商品流：** 让 Claude 用 Mock 数据生成商品列表，重点盯着 `<image>` 的 `mode="aspectFill"`。
3. **印章贴纸微调：** 实现商品图上绝对定位的圆形英文印章效果。
4. **今日检查点：** 首页和全部商品页在开发者工具里长得和设计图一模一样，且真机预览时不卡顿、图片不拉伸。

---

## 🛒 Day 3：完善长尾页面与细节微调（积分商城 + 购物车 + 我的）

第三天利用组件复用性，快速推进剩下三个页面，并做全面的 iPhone/安卓全面屏适配。

### 📌 阶段五：积分商城（Scores）手绘风还原（预计 2 小时）

1. **复用 Grid 布局：** 直接复用昨日的 2 列网格代码，快速改写卡片结构。
2. **代金券卡片上架：** 导入手绘线条代金券图片。

### 📌 阶段六：购物车（Cart）与吸底结算（预计 2 小时）

1. **购物车列表：** 实现自定义的圆形 Checkbox 和数量加减器。
2. **推荐商品横向滑动：** 编写 `scroll-view` 实现周边的横向滚动。
3. **结算栏吸底：** **核心安全区域注入**（`safe-area-inset-bottom`）。

### 📌 阶段七：我的（My）页面会员卡堆叠（预计 2 小时）

1. **背景与卡片错位堆叠：** 用负的 `margin-top` 和 `z-index` 让 VIP 卡片完美压在背景图上。
2. **清爽分割线列表：** 完成下方功能菜单。

### 📌 阶段八：全局走查与精细化 Debug（预计 1.5 小时）

* 在终端对 Claude 发起最后的微调微调指令（比如字体间距、点击态缩放动画等）。
* **今日检查点：** 5个页面全部完工，前端静态 UI 1:1 完美交付。

---

## 💡 落地执行小贴士：如何高效推进这份计划？

1. **坚持“单页闭环”原则：** 千万不要让 Claude 刚写完首页的轮播，就急着让他去写购物车的结算。每完工一个模块/页面，一定要在微信开发者工具里看一眼。确认好看、没问题了，再对 Claude Code 说：“*首页很完美，接下来我们按照计划进入 Day 2 的全部商品页开发*”。
2. **把报错直接丢给 Claude Code：**
如果遇到微信开发者工具控制台报红（例如 `wxml compile error` 路径找不到），直接把控制台的报错文字复制粘贴进终端，Claude Code 会自动定位文件并一秒修复。

这份计划和前面的终极指令是一对“双子星”。你现在就可以在本地新建好小程序项目，然后按照 **Day 1 阶段一** 的步骤，正式叫醒你的 Claude Code 开始大干一场了！