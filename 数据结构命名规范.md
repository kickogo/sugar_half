下面给你一套**微信小程序专用、覆盖前端/云函数/数据库/API 的数据结构命名规范**，直接能用、和官方习惯一致、避免踩坑（比如 dataset 大小写问题）。

---

## 一、核心原则（先记住这 3 条）
1. **前端 JS/JSON：小驼峰 camelCase**（userInfo、goodsList）
2. **WXML dataset：data-短横线**（data-goods-id → dataset.goodsId）
3. **数据库/云函数返回：**
   - 集合名：**小写+下划线**（goods、user_order）
   - 字段名：**小驼峰**（_id、goodsId、createTime）

---

## 二、前端数据结构（Page/Component 的 data、对象、数组）
### 1）普通对象（实体）
**小驼峰，语义完整，不缩写**
```js
// ✅ 推荐
const goodsInfo = {
  goodsId: "g001",
  goodsName: "奶油蛋糕",
  price: 199,
  stockNum: 10,
  coverUrl: "/images/cake1.jpg",
  createTime: "2026-05-30"
}

// ❌ 不推荐（下划线、拼音、缩写）
const goods_info = { ... }
const spInfo = { ... }
const danGao = { ... }
```

### 2）数组（列表）
**复数或 List 结尾**
```js
const goodsList = [ ... ]
const recommendGoods = [ ... ]
const cartItems = [ ... ]
```

### 3）布尔值
**is/has/can 开头**
```js
const isLoading = false
const hasStock = true
const canBuy = true
```

### 4）常量（配置/枚举）
**全大写下划线 UPPER_SNAKE_CASE**
```js
const API_BASE_URL = "https://xxx.com"
const MAX_GOODS_NUM = 99
const ORDER_STATUS = {
  PENDING: 0,
  PAID: 1,
  SHIPPED: 2
}
```

### 5）私有变量（页面/组件内）
**单下划线开头**
```js
Page({
  _timer: null,
  _internalData: {},
  ...
})
```

---

## 三、WXML 与 dataset 规范（你之前踩坑的重点！）
### 1）WXML 中 data-* 写法
**必须：data-小写-短横线，不要驼峰**
```html
<!-- ✅ 正确：data-短横线 -->
<view 
  wx:for="{{recommendGoods}}" 
  bindtap="onRecommendItemTap"
  data-goods-id="{{item.goodsId}}"
  data-goods-name="{{item.goodsName}}"
>
```

### 2）JS 中获取 dataset
**自动转小驼峰，全小写会被合并**
```js
onRecommendItemTap(e) {
  // data-goods-id → dataset.goodsId
  const goodsId = e.currentTarget.dataset.goodsId
  
  // ❌ 不要写 data-goodsId → 会变成 dataset.goodsid（全小写）
}
```

---

## 四、云函数（Node.js）数据结构
### 1）入参 event
**小驼峰，和前端一致**
```js
// 前端调用
wx.cloud.callFunction({
  name: "getRecommendGoods",
  data: {
    pageSize: 10,
    categoryId: "c001"
  }
})

// 云函数接收
exports.main = async (event) => {
  const { pageSize, categoryId } = event
  ...
}
```

### 2）返回数据（重要！你之前 id 缺失就在这）
**字段小驼峰，必须包含 _id 或 goodsId**
```js
// ✅ 正确返回（前端能拿到 id）
return {
  code: 0,
  list: res.data.map(item => ({
    goodsId: item._id, // 关键：映射 _id 给前端
    goodsName: item.goodsName,
    price: item.price,
    coverUrl: item.coverUrl
  }))
}
```

---

## 五、数据库集合与字段规范（云开发）
### 1）集合名
**小写+下划线，复数**
```
goods（商品）
user_cart（用户购物车）
order_list（订单列表）
```

### 2）字段名
**小驼峰，系统 _id 保留**
```
_id（系统自带，主键）
goodsId（商品ID）
goodsName（商品名）
price（价格）
createTime（创建时间）
```

---

## 六、API 接口数据（Request/Response）
### 1）请求参数
**小驼峰**
```js
// GET /api/goods?categoryId=c001&pageSize=10
```

### 2）响应体
**标准结构：code、msg、data**
```js
{
  code: 0, // 0成功，-1失败
  msg: "success",
  data: {
    goodsList: [ ... ],
    total: 100
  }
}
```

---

## 七、你项目的修正示例（直接套用）
### 1）云函数 getRecommendGoods 返回
```js
// 云函数
const res=await db.collection('goods').limit(10).get()
return {
  code:0,
  list: res.data.map(item => ({
    goodsId: item._id, // 必须返回，前端才能拿到 id
    goodsName: item.goodsName,
    price: item.price,
    coverUrl: item.coverUrl
  }))
}
```

### 2）前端 WXML
```html
<view 
  wx:for="{{recommendGoods}}" 
  bindtap="onRecommendItemTap"
  data-goods-id="{{item.goodsId}}"
>
```

### 3）前端 JS
```js
onRecommendItemTap(e) {
  const goodsId=e.currentTarget.dataset.goodsId
  console.log('goodsId =', goodsId) // 能正常打印
  if (!goodsId) return
  // ...
}
```
